- [x] `TeacherLayout.tsx` 的 HTML/JSX 结构修复，所有闭合标签正确无误，没有阻挡点击事件的隐形遮罩。
- [x] 侧边栏不再有重复的“跨班大乱斗”按钮。
- [x] 侧边栏成功添加了“多维任务树管理”的导航按钮。
- [x] 在 `/teacher/task-tree` 路由下，顶部的页面标题正确显示为“多维任务树管理”。
- [x] 教师端侧边栏的 UI 视觉效果得到了优化，毛玻璃、渐变背景和悬停动效自然且美观。
- [x] 所有侧边栏链接均可以成功点击，并顺利渲染出对应的子页面（`Outlet` 内容）。